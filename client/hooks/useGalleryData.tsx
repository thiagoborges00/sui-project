import { useSuiClient } from "@mysten/dapp-kit"

export const useGalleryData = () => {

    const client = useSuiClient();
    // parou no minuto 43
    //https://www.youtube.com/watch?v=d_XiWK64qGg

} 